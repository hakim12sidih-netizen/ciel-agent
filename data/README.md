# ── Runtime data ───────────────────────────────────────────
# Ce dossier contient les données générées à l'exécution.
# Il est versionné dans git (vide par défaut) et ignoré en partie
# par .gitignore (voir data/*.db, data/snapshots/, etc.)

data/identity/    # Identité Ed25519 + Noyau key
data/snapshots/   # Snapshots JSONL (Axiome γ)
data/logs/        # Logs d'exécution
data/cache/       # Cache de calculs
data/sessions/    # Sessions de transparence
